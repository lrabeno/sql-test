{
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3"
  },
  "name": "sql-test",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "nodemon server",
    "seed": "psql -d testdb -a -f ./test.sql"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
